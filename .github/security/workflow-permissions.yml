schema_version: "1"

workflows:
  label-classification.yml:
    permissions:
      pull-requests: write
    justification:
      reason: "Automatic label management"
      control_reference:
        - LG-008
